# 变更日志

> 按时间倒序记录用户可见变更。每条记录必须说明“原因”。

## 2026-08-02 · 用户会员等级筛选

- 变更：用户列表新增 `全部、V9、V8、V7、V6、V5、V4、V3、V2、V1` 会员等级筛选，支持横向滚动并可与状态筛选组合。
- 变更：Android Debug 包提升为 `0.1.4-debug build 5`。
- 原因：管理用户时可以快速定位指定会员等级，优先查看高等级用户。

## 2026-08-02 · Android 返回手势与 APK 命名

- 变更：用户详情页接入系统返回手势，边缘滑动会返回用户列表，不再退出 App。
- 变更：下载页版本展示和版本化 APK 文件名移除括号，改用 `build-N` 格式；Debug 包提升为 `0.1.3-debug build 4`。
- 原因：修复 Android 原生返回行为，并让内网发布物的版本标识更适合正式管理。

## 2026-08-02 · local233 管理端直连生产数据

- 变更：local233 管理后端改为经私有 SSH 隧道连接 `peiqian` 上的生产 `football_data`，启动时自动执行 migration。
- 变更：默认管理员调整为 `admin` / `admin123`，移除测试用户自动初始化，保留独立 Redis。
- 变更：Android 内网 Debug 包提升为 `0.1.2-debug (3)`，继续默认连接 local233 管理 API。
- 原因：让 Android 管理端在本机 API 上直接查看和管理真实用户，同时不开放生产 PostgreSQL 公网端口。

## 2026-08-02 · 原生 Android 管理端与独立 Admin API

- 变更：新增 Kotlin + Jetpack Compose 原生 Android 管理端，支持登录、用户管理、会员调整、审计、服务器切换和生物识别本地解锁。
- 变更：新增 `/api/v1/admin/**` 独立认证体系、可撤销管理员会话、owner 初始化和角色授权边界。
- 变更：用户详情新增邀请下级、活跃、订单、订阅和设备聚合；设备令牌仅返回掩码。
- 变更：用户禁用/恢复和会员调整均要求原因，并在同一数据库事务中写入审计。
- 变更：Android Release 默认使用 `https://match.oryjk.cn/`，Debug 默认使用 local233 测试 API，服务器切换会校验 Football Insight JSON 响应。
- 变更：新增 local233 内网 Android 下载页与发布脚本，提供 `latest.apk`、SHA256、发布说明和历史版本，不对公网发布。
- 原因：提供与 C 端完全隔离、可审计且适合当前单 owner 使用的移动管理工具，同时保留未来多管理员扩展边界。

## 2026-08-02 · local233 隔离测试环境（已由生产数据联调环境替代）

- 变更：新增独立 PostgreSQL、Redis 和后端测试栈，API 地址为 `http://172.16.60.233:18092/`。
- 变更：内网 Debug APK 默认连接测试环境，Release 构建仍默认连接生产环境，并允许登录前验证服务器。
- 原因：真机测试管理功能时必须与生产数据库和生产后端完全隔离。

## 2026-08-02 · 项目记忆初始化

- 变更：建立项目记忆文档。
- 原因：让 Agent 和项目导航能够快速恢复上下文。
