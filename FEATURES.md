# 项目功能清单

## 当前定位

- 项目定位：面向中国足球用户的数据洞察产品，包含数据抓取、Rust API、小程序/H5 和原生 Android 管理端。
- 当前状态：开发中

## 已完成

### 数据链路

- 新浪数据抓取、头像上传、PostgreSQL `f_i_*` 入库
- Rust/Axum 六边形架构 API
- uni-app 微信小程序与 H5

### Android 管理端

- 独立管理员账号、Admin JWT 与可撤销服务端会话
- Keystore 加密会话与可选生物识别本地解锁
- 用户搜索、状态/会员筛选和聚合详情
- 用户软禁用、恢复及 C 端会话撤销
- 带原因的会员等级/到期方式调整
- 管理操作审计列表
- 生产 HTTPS 默认地址与运行时服务器健康验证切换
- local233 管理联调环境：本地后端和独立 Redis，经私有 SSH 隧道连接生产数据库并自动执行 migration

## 进行中

### 管理端联调

- 功能：本地 API `http://172.16.60.233:18092/` 直接读取生产数据，默认管理员为 `admin`。
- 边界：真机安装验证和 UI 操作回归仍待用户执行；生产后端尚未部署或重启。

## 下一步

- 进行 Android 真机安装，验证局域网访问 `172.16.60.233:18092`
- 增加 Android 分页加载、审计筛选和更友好的 RFC3339 到期时间输入
- 完成真机网络与管理操作验收后再部署生产 Admin API
